# 📖 دليل التشغيل والتطوير لمنصة PROMET 🚀

مرحباً بك في الفريق المطور لمنصة **PROMET**! يهدف هذا الدليل إلى تزويدك بكافة التفاصيل التقنية اللازمة لفهم بنية النظام الأساسية، آليات الأمان، تحسينات الأداء، والسكريبتات الإدارية المتاحة لتسهيل مهام التطوير والصيانة اليومية.

---

## 🎯 1. البنية المعمارية للنظام (System Architecture)

تعتمد منصة **PROMET** على بنية متكاملة لربط خادم خلفي ديناميكي بواجهة أمامية حديثة وفائقة التجاوب:

*   **الواجهة الأمامية (Frontend):** مبنية باستخدام **React (v18+)** مدمجاً مع **Vite** للتجميع فائق السرعة، و**Tailwind CSS** للتنسيق البصري الفاخر والتصميم المتجاوب.
*   **الخادم الخلفي (Backend Server):** مبني باستخدام بيئة **Node.js** وإطار العمل **Express.js** المكتوب بلغة **TypeScript** بالكامل لضمان نمذجة البيانات المتينة وخلو الأكواد من الأخطاء النوعية.
*   **طبقة تخزين البيانات الدائمة والبديلة:**
    1.  **PostgreSQL (Cloud SQL):** قاعدة البيانات العلاقية الأساسية للشركة لإدارة شؤون الموظفين، كشوفات المرتبات، الحسابات والفواتير. تستخدم محرك الORM الموثوق لضمان كفاءة معالجة المعاملات.
    2.  **Firebase Firestore:** تستخدم بالتوازي لحفظ المستندات اللامنهجية وإدارة شؤون التدقيق والاتصال السريع للمهير والتقارير المتقدمة.
    3.  **Local Database fallback (`database.json`):** في حال عدم توفر اتصالات قواعد البيانات السحابية في بيئات التطوير المحلية، ينتقل النظام تلقائياً وبسلاسة إلى نظام ملفات JSON المحلي لضمان عدم توقف المطور عن العمل.

---

## 🔐 2. نظام التوثيق والأمان (Authentication & Refresh Tokens)

لتوفير أعلى مستويات الأمان ومكافحة ثغرات سرقة الجلسات، تم تطبيق بروتوكول تدوير رموز التحديث المحسن (**JWT Refresh Token Rotation**):

*   **Access Token:** رمز وصول قصير المدى (صلاحية **15 دقيقة**)، يتم تمريره عبر الترويسات لتفويض الطلبات اليومية.
*   **Refresh Token:** رمز تحديث طويل المدى (صلاحية **7 أيام**)، مخزن بشكل آمن في قاعدة بيانات المخدم بجدول `refresh_tokens`.
*   **آلية العمل:**
    *   عند انتهاء صلاحية Access Token، يستدعي العميل تلقائياً نقطة النهاية `/api/auth/refresh` ممرراً رمز التحديث.
    *   يتحقق المخدم من صلاحية الرمز ومطابقته بقاعدة البيانات، ثم يقوم **بإلغاء وتدمير الرمز القديم وتوليد رمز تحديث جديد تماماً وإرساله للعميل** جنباً إلى جنب مع Access Token جديد.
    *   تحمي هذه الطريقة التطبيق تماماً من هجمات تكرار استخدام الرموز المسروقة (Replay Attacks).

---

## ⚡ 3. محرك الذاكرة المؤقتة (Caching Engine)

لتحقيق سرعة استجابة مذهلة ومواجهة ضغط الاستعلامات المتكررة على قواعد البيانات، تم تصميم نظام كاش داخلي متقدم (`cacheService`):

*   **الموقع والملف:** `src/services/cache.service.ts`
*   **ميزات الخدمة:**
    *   تخزين مؤقت في الذاكرة الحية (In-Memory Cache) مع ميزة تحديد وقت العيش لكل سجل (**Time-To-Live - TTL**).
    *   إبطال وتطهير جزئي ذكي للبيانات المستهدفة بنظام البادئات (**Prefix-based Invalidation**)، مثل إبطال كافة مفاتيح المخزون المنتسبة للمنتجات مثل `products:*` فور حدوث أي عملية كتابة (إنشاء، تعديل، أو حذف منتج)، مع الإبقاء على باقي عناصر الذاكرة غير متأثرة.
    *   تفريغ فوري وتام لكافة أنواع الكاش (بما في ذلك كاش متصفحات الويب وكاش الطلبات الخلفية) عبر نقطة نهاية آمنة: `POST /api/cache/flush` متكاملة بصرياً في لوحة التحكم.

---

## 📊 4. فهارس تسريع الاستعلامات (Postgres Database Indexes)

تم تفعيل فهارس تسريع الأداء لضمان بقاء الاستعلامات الفردية والمركبة سريعة حتى مع نمو وتراكم البيانات:

1.  **جدول الموظفين (`employees`):** فهرس مركب على `[tenant_id, email]` وفهرس على `[status]`.
2.  **جدول كشوف المرتبات (`payroll`):** فهرس مركب على `[tenant_id, employee_id, month, year]`.
3.  **جدول الفواتير (`invoices`):** فهرس مركب على `[tenant_id, status]` وفهرس مركب على `[client_name, issue_date]`.
4.  **جدول المنتجات والمخازن (`products`):** فهرس مركب على `[tenant_id, category]` وفهرس على `[sku]`.
5.  **جدول رموز التحديث (`refresh_tokens`):** فهرس على حقل البحث السريع `[token]`.
6.  **سجلات المراقبة والتدقيق (`audit_logs`):** فهرس مركب على `[tenant_id, action_type, table_name]`.

---

## 🔧 5. نظام النسخ الاحتياطي اللحظي (Database Durability & Backups)

لحماية أصول الشركة ومعلوماتها المحاسبية الحساسة، تم تطوير سكريبت نسخ احتياطي تلقائي ونقطة إدارية مخصصة:

*   **المسار الإداري:** `/api/admin/backup`
*   **آلية العمل:** يقوم السكريبت بقراءة أحدث كشوفات المرتبات، الفواتير، الموظفين، والمصروفات، وتجميعها وتوليد ملف كائن منسق بهيكل تشفير نظيف، وتصديره كملف تنزيل فوري للمدير المسؤول.
*   **تشغيل يدوي:** يمكن تفعيله بضغطة زر من خلال قسم العمليات بلوحة التحكم المتقدمة.

---

## 📝 6. وثائق واجهة برمجة التطبيقات (API Documentation - Swagger)

تم توثيق كافة نقاط النهاية (Endpoints) باستخدام **Swagger** لتسهيل عمل المطورين الجدد واختبار الطلبات مباشرة:

*   **رابط التوثيق الفوري:** يمكنك الدخول إلى لوحة الاختبار التفاعلية عبر الرابط:
    `http://localhost:3000/api-docs`
*   يحتوي التوثيق على هيكلية ترويسات المصادقة المطلوبة، ونموذج الطلب والمخرجات لكافة العمليات الحسابية والمخزنية وعمليات الموظفين.

---

## 🏃‍♂️ 7. أوامر التشغيل الأساسية للتطوير والإنتاج

تأكد من تنصيب بيئة العمل ومن ثم تشغيل الأوامر التالية:

### أ. تثبيت الاعتمادات والحزم
```bash
npm install
```

### ب. تشغيل بيئة التطوير المحلية (Local Development)
يتم تشغيل الخادم والواجهة معاً على المنفذ الموحد `3000` مع مراقبة التعديلات الفورية:
```bash
npm run dev
```

### ج. فحص الكود والتأكد من توافق الأنواع (Linting)
```bash
npm run lint
```

### د. تجميع واختبار الكود للإنتاج والتشغيل الفعلي (Build & Production Start)
```bash
npm run build
npm start
```

*ملاحظة: تأكد دائماً من ضبط معطيات ملف الاتصال `.env` لبيئتك المحلية قبل محاولة ربط الخوادم السحابية المباشرة.*

**نتمنى لك رحلة تطوير موفقة وممتعة مع نظام بروميت! 💻✨**
